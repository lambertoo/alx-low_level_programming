#include <stdio.h>

/**
 * Main - Entry point
 *
 * Description:
 * Prints on the std output by only using puts() func.
 * Not allowed to use prinf() func
 * End the program with 0 value.
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\zle\n");

	return (0);
}
