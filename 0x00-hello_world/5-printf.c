#include <stdio.h>

/**
 * main - always the entry point
 *
 * Description: Print text using printf() fun.
 * Return: always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
