#include "is_palindrome.h"

/**
 * is_palindrome - returns 1 is the string is palindrome
 * and 0 if not.
 * @s: string to find if its palindrome
 *
 * Return: 1 if palindrome, 0 if not
 */
int is_palindrome(char *s)
{
	if (s == '\0')
		return (0);

}

/**
 * _strlen - count characters
 * @s: string var
 *
 * Return: char num
 */
int _strlen(char *s)
{
	
}
