#include <stdio.h>

/**
 * main - Entry point never disappoint
 *
 * Description: Print all numbers
 *
 * Return: 0 (success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n <= 9; n++)
		printf("%d", n);

	printf("\n");

	return (0);
}
